# 2026-09-15

- 将「260724 【人资】简历筛选」项目初始化为 git 仓库并推送至 GitHub：https://github.com/JerryFAZA/Resume-saver-screening.git（分支 main，首次提交 174 个文件）。
- 新增 `.gitignore`，忽略 `output/`（用户要求不上传）、`.venv/`、`__pycache__/`、`.codebuddy/`、`.workbuddy/`。
- 本地提交身份使用 `JerryFAZA` + `JerryFAZA@users.noreply.github.com`（仓库级配置）。
