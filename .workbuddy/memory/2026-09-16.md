# 2026-09-16

## 08:20 前批次收尾：26091517 ai产品经理 全链路完成（评估阶段收尾）

- 岗位：AI产品经理（正确 jobNumber=CC136786060J41031060702，薪资 12000-20000），输出目录 `output/26091517 ai产品经理`。
- 下载 200/200 docx（0 失败 0 跳过，3 次断点续传重启达成）；Skill 修复已落盘：#58 岗位校验失败→FATAL exit 1、#59 [D] 滚动改 CDP mouseWheel+scrollHeight 增长检测（scrollTop 赋值不触发懒加载）、#60 Send-Web curl -m 20 硬超时。
- 评估：prepare 200→185 行（去重15）；`_make_digest.py`（v1 逐字符 join bug 已修 v2）生成 `_digest.jsonl`；全量读 185 条写 `evaluations.json`（index 对齐）；`pipeline.py evaluate` 写入 + verify 通过；分布 **高13 / 中104 / 低68**。高匹配代表：戴洪远33、杜女士29、海先生30、李先生26、张先生29、董先生27、宋先生27、孔先生26、杨先生27、于女士33、牛女士35、李先生24、李先生34。
- 规则执行：年龄>35 封顶中；期望下限>20k 降级（祝先生32 海尔AIGC 强但期望3-3.5万→低）；在职-暂不找工作→低；意向地非青岛→低。
- 环境坑：PS `*>` 重定向写 UTF-16 致 Read 判二进制（转 UTF-8 BOM 可读）；Remove-Item 管道批量删除偶发静默失败，需 ForEach+ErrorAction Stop 重试；output/ 根目录历史 `_*.txt/_*.json` 诊断临时文件已清理（保留 _make_digest.py）。
- ⚠️ 注意：本日 08:26 另一会话批次记录使用 jobNumber=CC136786060J41007428302（该号对应 联想渠道经理-青岛），如继续该批次务必先按 #58 铁律核对岗位。

## 08:26 下载批次（用户手动停止）
- 用户指令：AI产品经理 岗位（jobNumber=CC136786060J41007428302）下载 200 份至 output/26091609 ai产品经理，config.json 已按铁律 #58 采用用户提供的 jobNumber 更新（DownloadCount=200）。
- WbResumeDL 计划任务 08:26:46 启动，约 2 分钟后用户叫停；已 Stop-ScheduledTask + kimi-webbridge stop。已下载 3 份 docx。
- 重启要点：config DownloadCount 改为 200-已有份数（断点续传按 姓名_年龄 预填），Start-ScheduledTask 即可。评估未开始。

## 08:40 skill 修复调优：#61 选岗验证闭环 + #62 停滞自动重启（用户指令）

用户重申两问题并要求升级修复：问题1"收到指令后没确认岗位就下错"→ 应**在网站先选择正确的岗位标签卡**；问题2"还有大量简历时卡住"→ **卡住时应重启循环**。

- **#61（问题1）**：根因=旧 Select-JobTab 只发一次 JS click 即返回成功、不验证激活岗位是否切换（Vue 可能忽略合成 click）。重构为：精确匹配优先（includes 兜底）→ data-wb-job 打标 → **三级点击升级（JS click→DOM click→CDP 真实鼠标），每级后 Test-JobActive 复核激活岗位名确实切换**；选岗成功读 location.href 的 jobNumber 写 _summary.json（jobNumber 字段）溯源。config URL 带错号也会被选岗纠正。
- **#62（问题2）**：三层修复——① run.ps1 停滞看门狗：ok+fail+skip+dup 连续 300s（StallTimeoutSec）无变化 → status=STALL → **exit 4**（滚动硬上限/连续5轮异常也归入 stall；仅池耗尽保持 exit 2）；② wrapper 重写为最多 8 轮自动重启循环：exit 0/1/2 停止、3/4 重建 daemon 续跑，轮次写 %TEMP%\wb_wrapper_status.txt，日志改追加带轮次分隔；③ config 新增 **DownloadTarget**（总目标恒定），run.ps1 启动自动折算 本次目标=总目标-已有份数，重启免手工改 DownloadCount。
- 文件改动：`scripts/lib/zhaopin-page.ps1`（Test-JobActive/Remove-JobMark 新增+Select-JobTab 重构）、`scripts/run.ps1`（看门狗+exit 4+summary 增 dup/jobNumber）、`%TEMP%\wb_run_wrapper.ps1` 重写（模板存 `scripts/wb_run_wrapper.template.ps1`）、`scripts/config.json`（**jobNumber 纠正为 CC136786060J41031060702** + DownloadTarget=200）、SKILL.md 5 处。全部 parse_errors=0 + BOM EF-BB-BF + Grep 复核落盘。
- ⚠️ 26091609 目录中已有 3 份 docx（08:27-08:29）来自**错误岗位池（联想渠道经理）**，继续该批次前应删除，否则断点续传会把它们当作已下载并混入批次。

## 09:00 skill 启动提速：#63 发布任务→正式执行等待链优化（用户指令）

用户反馈"每次发布任务到正式执行前的等待时间太久"。根因（按耗时排序）：① wrapper 每轮无条件重建 daemon（stop 2s + start 后扩展重连最坏 200s）② wrapper 探测先睡 5s 再探测 + ok 后再睡 3s（冗余）③ run.ps1 导航后固定 3s×2、选岗后 3s、回顶后 2.5s（下游均有条件门兜底）④ 环境自检 daemon 启动固定 4s、扩展探测固定 5s 间隔。

修复（全部"条件满足立即放行"化）：wrapper 第 1 轮先快速探测（3×800ms）扩展活着→跳过重建直接跑，第≥2 轮仍强制重建（保留 exit 3/4 卡死恢复）；探测自适应间隔（wrapper 0.5s→1s→2s→5s 上限 ~153s；wb-core 1s→3s→5s 上限 230s）；删 ok 后 3s、轮间 5s→2s；run.ps1 导航 3s×2→800ms、选岗后 3s→1s、回顶 2.5s→1.2s；daemon 启动固定 4s→500ms 端口轮询（上限 8s）。效果：健康路径省 15-20s，daemon 已就绪时省扩展重连（10-200s 量级）。**铁律不动**：ScrollWaitMs/SaveCloseWaitMs/ModalRetryWaitMs 等 #53/#57 用户指定参数未触碰。

文件改动：`scripts/wb_run_wrapper.template.ps1`（重写，**已同步 %TEMP%\wb_run_wrapper.ps1**，字节数一致）、`scripts/run.ps1`、`scripts/lib/wb-core.ps1`、SKILL.md（问题记录 #63）。全部 Parser::ParseFile 语法 0 错误 + Grep 复核落盘。

## 09:26 skill 调优：#64 网页崩溃自动重载（用户指令）

用户反馈刷简历时网页会崩溃，要求崩溃后自动重新加载。旧自愈两大缺陷：① 只认 ok:false 且用 navigate newTab=$false——崩溃 tab 上原地导航无效，恢复不了；② 恢复后不复核激活岗位（重载后推荐池可能已变，#61 事故复发风险）；空/无效响应静默当"视口无卡片"空转，最坏等 300s 看门狗。

修复（三层）：① 主循环 [A] 崩溃检测门：ok:false 或无 ok:true（空/无效响应）均判崩溃；② `Repair-PageCrash`（zhaopin-page.ps1 新增）：newTab=$true 重建标签页 → 等列表渲染 30s → **岗位复核**（不匹配→重新 Select-JobTab 三级选岗）→ 回顶恢复（$processed 保留防重复下载）；③ 面板阶段：save-local 30s 未现先 Test-PageAlive，崩溃则修复并**取消卡片登记**（保存未发生，恢复后重试）；保存点击之后的位置不回退登记（防重复文件）。熔断：连续 3 次修复失败 → exit 3 交 wrapper 重建 daemon（daemon 死亡场景 2-3 分钟自愈，不再等 300s）。

文件改动：`scripts/lib/zhaopin-page.ps1`（Test-PageAlive/Repair-PageCrash 新增）、`scripts/run.ps1`（[A] 检测门 + 面板接入 + consecRepair 计数器）、SKILL.md（#64）。语法 0 错误 + Grep 复核。

## 10:00 26091609 ai产品经理 批次全链路完成（本会话）

- 指令：AI产品经理（jobNumber=CC136786060J41031060702，薪资 12000-20000）下载前 100 条 → output/26091609 ai产品经理 → 评估生成 ai产品经理.xlsx。
- **修复 #65（wrapper 假 exit 0）**：Start-ScheduledTask 后 26 秒报 exit=0 但无产物——根因=wrapper 模板只写注释没实现路径表读取，`$runPs`/`$logPath` 为 null，`& $runPs` 静默失败、`$LASTEXITCODE` 沿用 daemon start 的 0。修复=模板+部署版补 `ReadAllLines` 读 wb_wrapper_paths.txt + 前置校验。教训：**"秒完成的成功"必须核查产物而非只看退出码**。
- **修复 #66（岗位标签小写+点号后缀）**：页面标签实际为"ai产品经理·协作未上线"，JS ===/includes 大小写敏感失配 + Test-JobActive 剥不掉"·"后缀 → 选岗 FATAL exit 1。修复=findJs 两侧 toLowerCase + Test-JobActive 增剥 `\s*·.*$` 与 -ieq/ToLower().Contains。
- ⚠️ 并行会话撞号事故：本会话向 SKILL.md 追加问题记录时，与并行会话 09:00/09:26 写入的 #63（启动提速）/#64（崩溃重载）撞号，已重编号为 #65/#66 并物理移至 #64 后（60-66 顺序、BOM 完好）。**多会话并行改同一 SKILL.md 必须先 Grep 最新编号再追加**。
- 下载：**100/100 DONE**（0 dup、1 fail 补位，09:02-09:36 约 34 分钟），_summary.json jobNumber 溯源正确。
- 评估：prepare 100→88 行（去重 12）；_digest.jsonl（复用 _make_digest.py）→ 全量评估 88 条写 evaluations.json → pipeline evaluate + verify 通过，分布 **高6/中53/低29**，缺评 0。高匹配：于女士33（B端+AI生图0-1）、宋女士31（数字化+AI大模型落地）、张先生29（RAG/LLM+Agent）、李先生34（以萨AI产品5年+，疑似用户本人简历出现在推荐池）、杜女士29（AI管理平台+单据识别智能体）、阮先生27（智能码头AI产品线+售前）。
- 规则执行同前：年龄>35 封顶中；期望下限>20k 降级（祝先生32 海尔AIGC 强但期望3-3.5万→中待沟通）；暂不找工作→低；意向地非青岛→低；求职方向为技术岗→低。
