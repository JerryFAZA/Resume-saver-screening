# 2026-09-16

## 08:20 前批次收尾：26091517 ai产品经理 全链路完成（评估阶段收尾）

- 岗位：AI产品经理（正确 jobNumber=CC136786060J41031060702，薪资 12000-20000），输出目录 `output/26091517 ai产品经理`。
- 下载 200/200 docx（0 失败 0 跳过，3 次断点续传重启达成）；Skill 修复已落盘：#58 岗位校验失败→FATAL exit 1、#59 [D] 滚动改 CDP mouseWheel+scrollHeight 增长检测（scrollTop 赋值不触发懒加载）、#60 Send-Web curl -m 20 硬超时。
- 评估：prepare 200→185 行（去重15）；`_make_digest.py`（v1 逐字符 join bug 已修 v2）生成 `_digest.jsonl`；全量读 185 条写 `evaluations.json`（index 对齐）；`pipeline.py evaluate` 写入 + verify 通过；分布 **高13 / 中104 / 低68**。高匹配代表：戴洪远33、杜女士29、海先生30、李先生26、张先生29、董先生27、宋先生27、孔先生26、杨先生27、于女士33、牛女士35、李先生24、李先生34。
- 规则执行：年龄>35 封顶中；期望下限>20k 降级（祝先生32 海尔AIGC 强但期望3-3.5万→低）；在职-暂不找工作→低；意向地非青岛→低。
- 环境坑：PS `*>` 重定向写 UTF-16 致 Read 判二进制（转 UTF-8 BOM 可读）；Remove-Item 管道批量删除偶发静默失败，需 ForEach+ErrorAction Stop 重试；output/ 根目录历史 `_*.txt/_*.json` 诊断临时文件已清理（保留 _make_digest.py）。
- ⚠️ 注意：本日 08:26 另一会话批次记录使用 jobNumber=CC136786060J41007428302（该号对应 联想渠道经理-青岛），如继续该批次务必先按 #58 铁律核对岗位。

## 08:26 下载批次（用户手动停止）
- 用户指令：AI产品经理 岗位（jobNumber=CC136786060J41007428302）下载 200 份至 output/26091609 ai产品经理，config.json 已按铁律 #58 采用用户提供的 jobNumber 更新（DownloadCount=200）。
- WbResumeDL 计划任务 08:26:46 启动，约 2 分钟后用户叫停；已 Stop-ScheduledTask + kimi-webbridge stop。已下载 3 份 docx。
- 重启要点：config DownloadCount 改为 200-已有份数（断点续传按 姓名_年龄 预填），Start-ScheduledTask 即可。评估未开始。

## 08:40 skill 修复调优：#61 选岗验证闭环 + #62 停滞自动重启（用户指令）

用户重申两问题并要求升级修复：问题1"收到指令后没确认岗位就下错"→ 应**在网站先选择正确的岗位标签卡**；问题2"还有大量简历时卡住"→ **卡住时应重启循环**。

- **#61（问题1）**：根因=旧 Select-JobTab 只发一次 JS click 即返回成功、不验证激活岗位是否切换（Vue 可能忽略合成 click）。重构为：精确匹配优先（includes 兜底）→ data-wb-job 打标 → **三级点击升级（JS click→DOM click→CDP 真实鼠标），每级后 Test-JobActive 复核激活岗位名确实切换**；选岗成功读 location.href 的 jobNumber 写 _summary.json（jobNumber 字段）溯源。config URL 带错号也会被选岗纠正。
- **#62（问题2）**：三层修复——① run.ps1 停滞看门狗：ok+fail+skip+dup 连续 300s（StallTimeoutSec）无变化 → status=STALL → **exit 4**（滚动硬上限/连续5轮异常也归入 stall；仅池耗尽保持 exit 2）；② wrapper 重写为最多 8 轮自动重启循环：exit 0/1/2 停止、3/4 重建 daemon 续跑，轮次写 %TEMP%\wb_wrapper_status.txt，日志改追加带轮次分隔；③ config 新增 **DownloadTarget**（总目标恒定），run.ps1 启动自动折算 本次目标=总目标-已有份数，重启免手工改 DownloadCount。
- 文件改动：`scripts/lib/zhaopin-page.ps1`（Test-JobActive/Remove-JobMark 新增+Select-JobTab 重构）、`scripts/run.ps1`（看门狗+exit 4+summary 增 dup/jobNumber）、`%TEMP%\wb_run_wrapper.ps1` 重写（模板存 `scripts/wb_run_wrapper.template.ps1`）、`scripts/config.json`（**jobNumber 纠正为 CC136786060J41031060702** + DownloadTarget=200）、SKILL.md 5 处。全部 parse_errors=0 + BOM EF-BB-BF + Grep 复核落盘。
- ⚠️ 26091609 目录中已有 3 份 docx（08:27-08:29）来自**错误岗位池（联想渠道经理）**，继续该批次前应删除，否则断点续传会把它们当作已下载并混入批次。
