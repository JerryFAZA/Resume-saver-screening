# 2026-09-15 工作日志

## 联想渠道经理-青岛 简历下载与评估第二批（26091512，新岗位编号 CC136786060J41007428302）

- 任务：推荐页下载「联想渠道经理-青岛」前 100 份简历（word）至 `26091512\`，评估生成 `26091512\联想青岛.xlsx`。JD 薪资 6000-10000/月，职责为经销商维护开拓/产品方案推荐/项目运作支持/销售流程执行/货款回收。
- 首次运行 FATAL「Job tab not found」，深挖出 4 个环境级根因（已固化进 zhaopin-resume-saver SKILL.md「环境前置检查」+ 问题 #47-#50）：
  1. **HTTP_PROXY=127.0.0.1:12887 劫持 curl 对 10086 的请求**，返回伪装 daemon 错误 "upstream connect failed (os error 10061)"。修复：`$env:NO_PROXY="127.0.0.1,localhost"` / curl `--noproxy "*"`。
  2. **daemon 随父命令进程被清理**：一次性命令 start 的 daemon 约 1 分钟后静默消失（日志无崩溃）。修复：用 run_in_background 常驻任务承载 daemon（start + Start-Sleep）。run.ps1 末尾 Stop-BrowserAutomation 会 daemon stop，属预期。
  3. **Out-File utf8 带 BOM**：WebBridge 请求体报 `invalid character '茂'`。修复：`[System.IO.File]::WriteAllText` + `UTF8Encoding($false)`；响应用 `curl --output` 直写文件（Out-String 会 GBK 双重破坏中文）。
  4. **daemon v1.11.3 与扩展 v2.0.9 版本错配**：`kimi-webbridge upgrade` 对齐后正常。
- 修复后正常：岗位验证通过（联想渠道经理-青岛·协作未上线），收集 **152 位唯一候选人**，目标 100 份，约 37 秒/份。
- 评估管线要点：`fill_excel_template(json, xlsx)` 是**就地覆盖**传入文件——需先把模版复制为 `26091512\联想青岛.xlsx` 再填充；字段名 `filename`；按行索引写 B/C（row=idx+2）。
- **SKIP 风暴（用户反馈"拉到底再重置往复循环，未下载新简历"）**：idx 43-50 连续 7 个 `[SKIP] Not found after full-list sweep`。根因：收集的姓名快照过期（推荐列表动态重排，旧名字已不在列表中），每人 25 次重试 × 900ms 全列表扫描浪费约 40 秒。实测 idx 51 自愈（张女士成功）。修复：run.ps1 新增 `$consecSkip` 计数（问题 #51）——连续 ≥8 SKIP 即 break 内层循环提前进入 TOP-UP 重收名单。当前运行实例不受影响（PS 启动时已解析进内存），下次运行生效。截至 12:50 已下载 44/100。
- **SKIP 风暴二次发生 + 断点续传修复（问题 #52，用户再次反馈空转）**：idx 56-61 连续 6 个 SKIP（45/100 卡住）。教训：**运行中 PS 脚本热改无效**——#51 修复写进文件但旧实例仍跑旧逻辑。处置：TaskStop 停止 → 三处修改 → 重启：① sweep 重试 25→10（单人 SKIP 代价 40s→15s）；② **断点续传**：启动时扫 DownloadDir 已有 docx 文件名首段（姓名）预填 `$triedCandidates`，日志 `[RESUME] N resume(s) already in target dir`；DUP 三重验证兜底同名风险（实测 45 份无重名）；③ config DownloadCount 100→55（45 已有 + 55 新 = 100）。重启后验证：`[RESUME] 45` 出现、已下载姓名被静默跳过、新简历正常落盘。收集阶段因目标降为 55 只收 114 个名字（更快）。
- **13:48 用户叫停下载**，停止时已落盘 **87/100** 份。评估（联想青岛.xlsx）未开始，待用户指示。
- **13:49 环境排障沉淀至 skill（用户指示）**：SKILL.md「环境前置检查」新增 **E. 标准启动序列**（一站式 checklist：stop→upgrade 版本对齐 → run_in_background 常驻承载 daemon（start+Sleep 7200）→ --noproxy 循环探测 10086 就绪 → 设 NO_PROXY 启动 run.ps1）和 **F. 中断续传**（重启前 DownloadCount=目标-已有份数；[RESUME] 自动跳过；连续 SKIP→TOP-UP 是预期自愈行为）；「快速运行」步骤 2/5 改为指向 E/F，消除裸 start 旧写法。至此 #47-#52 全部问题在 SKILL.md 中既有问题记录又有可执行操作指引。
- **13:52-14:00 下载主循环重构（问题 #53，用户指定流程）+ 阶段 2 滚动提速**：
  1. `ScrollWaitMs` 1200→**800ms**（用户指定，阶段 2 滚动一屏等待）。
  2. **主循环改为「卡片登记 + 顺序推进」，下载后不回滚列表顶部**：① 点开前 DOM 提取卡片关键信息（姓名+年龄，key=`姓名_年龄`）；② 处理完毕（成功/失败/重复）立即登记 `$processed`；③ 关模态后从当前位置直接找下一条未登记卡片点击，不回顶；④ 视口消化完才下滚一屏（800ms）；⑤ 到底+连续 3 轮无新卡片→回顶重扫，连续 3 空轮才停（保留 #33 精神）。
  3. 配套：断点续传升级为 姓名+年龄 精确预填；删除 `$triedCandidates`/双循环/补收名单；阶段 2 全列表预收集简化为视口健康检查（省 30~90s）；新增 `Get-CardMarkJs` 函数（姓名 \uXXXX 转义+年龄双校验打标记）。已知限制：同名同龄不同人视为同一卡片（与文件三重去重口径一致）。
  4. 验证：Parser::ParseFile 语法 0 错误；`$triedCandidates` 残留 0 处（期间再次遇到 Edit 报成功未落盘，Grep 复核发现并补改——第 3 次验证该坑）；SKILL.md 同步（阶段 2/3 章节 + 问题 #52/#53 记录）。
- **14:42-15:00 卡片标识升级（问题 #54，用户指定）**：姓名+年龄 极易重复（脱敏名"张先生"大量同龄卡），key 加入**工作经历摘要**（容器文本剔除姓名/"N岁"/易变时间词（刚刚/N分钟前/N小时前/N天前/昨天/在线/活跃/看过等）后取前 30 字符）→ key=`姓名_年龄_工作经历摘要`。要点：① 剔除时间词是关键——推荐列表"12分钟前看过"随时间变化，混入 key 会跨重扫失配反复点击；② 标记 JS 与提取 JS 必须用**完全相同的归一化链**（否则摘要跨被剔除词拼接时 indexOf 失配）；③ 断点续传改 `$prefilled`（姓名_年龄，文件名不含工作经历）+ `$processed`（完整 key）双表过滤。**Node mock DOM 冒烟测试通过**（两个"张先生/34岁"不同公司正确区分、标记精确命中），测试抓到并修复 `join("").`+`.replace` 双点拼接 bug——Invoke-Eval 会静默掩盖 JS 语法错误，mock 测试是有效验证手段。SKILL.md #54 已记录，临时文件已清理。
- **15:00-15:36 下载环境排障 + #55 保存流程提速**：
  - **26091515 测试任务（下载 20 份）排障**：① 会话内后台任务（含非沙箱 run_in_background）约 2 分钟被宿主强杀，daemon 连带死亡（坑 B 复现）→ 沙箱内跑不完 15-25 分钟的批量任务；② schtasks /create 报 exit -1（参数处理问题），改用 `Register-ScheduledTask` + `Start-ScheduledTask` 成功——**计划任务独立进程树是本会话跑长任务的唯一可靠方式**；③ 探测脚本曾被糊弄：daemon 存活但扩展未连接时 snapshot 返回业务错误 "no tab"，被误判就绪 → **真探测必须用 `list_tabs`**（扩展在线才返回 ok:true），6 次 stop/start 后扩展掉线需耐心轮询等重连（实测 round 6 重连）。
  - **#55 保存流程提速（用户指定：release 保存按钮后 1000ms 关闭详情面板）**：原流程 release 后阻塞等 DownloadWaitMs（word 强制 10s/人）。新流程：release → Wait 1000ms → 立即 Close-ModalIfOpen + `.km-modal__close-btn` DOM click 关面板 → 轮询检测落盘（word 25s/pdf 15s 窗口每秒一查，`LastWriteTime > clickTime-5s` 命中）。**配套**：原 3 次保存重试 for 循环整体移除（面板关闭后无法重点保存）；命中基准改用点击前 `$clickTime`。验证：语法 0 错误、confirmRetry 残留 0 处。
  - **Edit 工具"报成功未落盘"第 4 次复现**：SKILL.md 表格行与 #55 记录首次 Edit 均未写入，Grep 复核发现后补改成功——纪律再次确认：SKILL.md 每处修改必须 Grep 复核。
  - 计划任务 WbResumeDL 运行中（旧脚本在内存），#55 对其不生效，下次运行生效；进度检查点 15:36 时约 8/19。
- **15:44-16:00 ★ skill 整体重构（用户指令：固定流程沉淀成脚本、skill 只留经验、增强稳定性）**：
  - **saver 新架构**：`scripts/lib/wb-core.ps1`（通用层：WebBridge 客户端 + Write-Log 结构化日志 UTF-8 落盘 + Invoke-WithRetry/Wait-Until deadline 制等待原语 + Initialize-WebBridgeEnv 环境自检（list_tabs 真探测）+ Stop-BrowserAutomation + 标签页管理）+ `lib/zhaopin-page.ps1`（页面层：选岗/验证、卡片提取三重标记、模态与保存序列、文件移动去重）+ `run.ps1` 薄编排（约 600 行，从 1337 行单体重写）。
  - **稳定性增强**：环境自检内建（失败 exit 3）；全关键等待 deadline 化；主循环 try/catch 单轮异常保护（连续 5 轮才停）；Move-OneResume 重试 3 次；产物 `_summary.json`（机器可读 DONE/INCOMPLETE）+ `_run_log.txt`（替代 *> UTF-16 痛点）；退出码 0/1/2/3。
  - **screening**：新增 `scripts/pipeline.py`（prepare/evaluate/verify 子命令）——复制模版→提取→填表→导出 rows.json（行序基准）→AI 评估 JSON→按行索引写 B/C+色阶→自动读回校验；Agent 只产 evaluations.json。
  - **文档**：saver SKILL.md 893→约 460 行（流程伪代码→脚本调用说明；经验/问题记录 #23-#56 全保留；新增长任务调度经验/时序参数表；E 节探测改 list_tabs；F 节更新断点续传口径）；screening SKILL.md 工作流程章节改 pipeline.py。删除弃用文件 config.ps1/webbridge-utils.ps1/download-loop.ps1/fix_config.py。
  - **验证**：3 个 ps1 补 UTF-8 BOM（PS 5.1 中文铁律）后 Parser 解析 0 错误；pipeline.py 编译通过；点源加载 + ConvertTo-JsSafeName/Get-CardMarkJs(无双点)/Wait-Until/Invoke-WithRetry 冒烟全过。
  - **工具新坑（重要）**：① PS 工具 WriteAllLines 写 SKILL.md 报成功但未落盘（第 5 次落盘事故，Edit/Write/PS 三条写入路径都中招过）——**所有关键写入后必须重新读取复核**；② safe-delete/trash 对中文路径报 GBK mojibake 失败但实际可能已删除，需复核；③ Bash 工具本会话退化（coreutils 丢失）不可用。
- **⚠️ 16:00 发现简历数据文件夹消失（待用户确认）**：26091512（48 份 docx）、26091515（8+ 份 docx + 日志）、260914 整个文件夹从项目目录消失，回收站无痕迹，System32 无误移，26091512 Glob 报"路径不存在"。.workbuddy/.venv/output/EvaluationTemplate.xlsx 完好。发生时间在 15:36-15:44 之间（用户报告"不动了"之后），非本会话任何操作所为（本会话未对这些目录做过删除/移动；计划任务只写入不删除）。**已提醒用户核实是否自行移动/清理，尚未确认。**


## 模版归档入 skill（第六轮）

- 确认：生成评估文档**需要**参考 EvaluationTemplate.xlsx（skill 的 fill_excel_template 以其为底版），不删除。
- 模版已按 skill 最新规则同步：12 列（无「建议行动」）、列宽 A/C=36 / K/L=50 / 其余 12、全表垂直居中+自动换行、冻结 A2、筛选 A1:L12；读回校验 0 异常。
- **模版副本已归档**：`.workbuddy/skills/zhaopin-resume-screening/templates/EvaluationTemplate.xlsx`；SKILL.md 更新为「默认使用 skill 内置模版，用户另行提供时以用户为准」。

## 列宽规则（第五轮）

- 用户指定列宽：A 简历名称 / C 简要评价 = 36 字符，K 求职意向 / L 工作经历 = 50 字符，其余 = 12 字符。
- 已应用到 `260914\联想青岛.xlsx`（读回校验 12 列全部匹配）；沉淀入 skill：`fill_excel.py` 新增 `COLUMN_WIDTHS` 常量并在 `fill_excel_template` 末尾强制覆盖模版原列宽，`SKILL.md` 模版结构段补充列宽规则；py_compile 通过。临时文件已清理。

## 删除建议行动列 + 重复简历去重 + 沉淀入 skill（第四轮）

1. **EvaluationTemplate.xlsx 删除 D 列「建议行动」**：模版变为 12 列（简历名称/胜任力评级/简要评价/年龄/性别/工作年限/现居住地/学历/求职状态/期望薪资/求职意向/工作经历）。
2. **260914\联想青岛.xlsx 同步**：删除 D 列；按「姓名+年龄」（从文件名正则解析）去重，110 行 → 90 行唯一简历（剔除 21 行重复），评级 高 8 / 中 52 / 低 29；垂直居中+自动换行全表保持，读回校验 0 异常、0 重复。
3. **skill 沉淀**：
   - `fill_excel.py`：TEMPLATE_HEADERS 12 列；新增 `dedup_resumes()`（姓名+年龄键，保留首份）并在 fill_excel_template 内置调用；write_evaluation_row 去掉 action 参数只写 B/C；CENTER_COLS 调整为 [2,4,5,6,7,8,9,10]；auto_filter A1:L；py_compile 通过。
   - `SKILL.md`：模版结构 13→12 列、删除「建议行动写法」章节（结论性建议并入 C 列说明）、新增「重复简历去重（⭐ 强制）」规则、流程图步骤3/4/5 更新为去重与 B/C 口径。
   - 踩坑：SKILL.md 含弯引号的行 Edit 匹配失败，改用 python 正则替换成功；上轮一处 Edit 报成功但未落盘（D 列行），本轮读回时发现并补改——**关键编辑后必须 Grep/Read 复核**再次验证。
4. 旧 8 列兼容路径（fill_excel/write_evaluations）未动。

## 全表自动换行 + 规则沉淀（第三轮）

- 用户要求所有单元格自动换行。复核 `260914\联想青岛.xlsx`：1443 个单元格已全部满足 wrap_text=True + vertical=center（编辑器归一化后换行已生效，读回校验 0 异常，无需再写）。
- **沉淀入 skill**：`fill_excel.py` 模版路径（fill_excel_template 三种对齐 + write_evaluation_row 的 B/C/D）全部显式加 wrap_text=True；`SKILL.md` 填充方式规则更新为「垂直居中 + 自动换行，无一例外，不许 vertical=top 或无换行」。
- 旧 8 列兼容路径（fill_excel/write_evaluations）保持原样未改。临时文件已清理。

## 联想渠道经理-青岛 简历下载补足至 100 份 + 全量重新评估（260914）

### 背景：用户两次质疑与根因修复

用户指出上一轮（09-14）的两个严重错误：
1. **「明明有非常多简历，为什么你说才31份」** —— 把脚本自身的滚动缺陷误判为「推荐池只有 31 位唯一候选人」。
2. **「你一直在重复循环翻候选人列表，但没点进去任何一个候选人详情」** —— 下载阶段空转，从第 37 位起全部 `[SKIP] Not found in viewport`。

### 根因分析（两个 bug）

**Bug 1：姓名收集阶段误判"已到底"**
- 原实现用 CDP `mouseWheel` 在固定坐标 (500,600) 滚动，对虚拟滚动列表不可靠。
- 停滞阈值 `$stag -ge 3` 过于激进，在第 11 次滚动就判定到底，实际列表有 **142 个唯一姓名**。
- **修复**：改为 JS 直接滚容器 `document.querySelector('.app-layout--default').scrollTop += 900`；停滞阈值提高到 **12 轮**；并加 `$maxScrolls = 80` 硬上限防死循环。

**Bug 2：下载阶段从底部开始"往下找"，永远找不到**
- 收集阶段把列表滚到了底部；下载阶段**未重置回顶部**，且查找循环只往下滚（`DeltaY 600`），已在底部无法再下滚 → 15 次重试原地打转。
- **修复**：① 收集完姓名后、进入下载前执行 `scrollTop = 0` 归零；② 每个候选人的查找循环**第 0 轮先归零到顶部**再逐屏下滑；③ 重试上限 15 → **25 次**。

**修复后验证**：姓名收集 139 个；原先 `[SKIP]` 的 吴先生/陈先生/高先生 全部成功下载；全程 **0 失败 0 跳过**。

### 执行结果

- **下载 100 份**（第三轮，约 38 分钟，Word 模式，约 23 秒/份），`260914/` 实际 100 个 .docx。
- **评估结果：高 9 / 中 74 / 低 17**。
- 交付 `260914\联想青岛.xlsx`（100 行 + 表头，B 列色阶 绿 C6EFCE / 黄 FFEB9C / 红 FFC7CE）。

### 高匹配名单（9 人，全部青岛、年龄≤35）

| 姓名年龄 | 匹配理由 |
|---|---|
| 刘先生_32岁 | IT 软硬件金牌代理商/分销商（用友/金蝶/戴尔/华为），技术销售总监 |
| 张先生_31岁 | 家电经销商渠道自主开发 + 机房设备项目销售 |
| 周先生_29岁 | LED 显示行业区域总监 + 城市经理 |
| 姜先生_35岁 | 家电/空调渠道销售经理（美的/海尔体系） |
| 苏先生_28岁 | 医疗器械渠道拓展（签约 10 家区域代理商 + 电商渠道） |
| 陈先生_28岁 | 消费电子渠道（韶音耳机/运动对讲机门店开发与渠道维护） |
| 隋先生_33岁 | 智能硬件渠道体系构建（迈金智能） |
| （另含 2 个与上列同候选人的重复下载条目） | |

### 重点"中"档（IT/硬件高度相关，仅因超龄受限）
- **穆先生_36岁**：大华 + 华硕 + 英特尔，IT 硬件渠道最强，超龄 + 薪资略超
- **樊先生_38岁**：**联想 ThinkPad 督导 + 联想经销商对接**，原厂体系直接经验，超龄 + 现居济南
- **薛先生_45岁**：系统集成项目经理（教育/政府/央国企软硬件投标，年 3000 万）
- **庄先生_49岁**：以萨技术 + 海信网络科技系统集成销售
- **袁先生_36岁**：爱谱华顿弱电智能化 + 捷顺科技智慧停车

### 数据质量提示
- 推荐池中存在**同名同年龄候选人重复出现在推荐列表**（如 安先生_37岁、樊先生_38岁、陈先生_28岁 等约 20 组），Excel 中已逐条如实标注「与上一条为同一候选人重复」。
- `M先生_24岁` 为**英文版简历**（博士在读，中国石油大学软件工程，求职外贸销售），提取器未解析出姓名以外的信息，已人工判定为"低"。

### 新增技术踩坑
1. **`extract_docx.py` 输出的字段名是 `filename` 而非 `file_name`**，读 JSON 时注意。
2. **`write_evaluations()` 用「姓名+年龄」子串匹配 A 列，遇到同名同年龄重复候选人会串行写错**。本轮改为**按行索引**（row = JSON索引 + 2）直接写入，精确无误。已在 SKILL.md 记录。
3. PowerShell 控制台输出中文会显示为乱码（`M鍏堢敓`），但**文件内容是正常的 UTF-8**，不要据此判定编码损坏——必须读文件确认。

### 交付物
- `260914\联想青岛.xlsx`（100 行评估表）
- `260914\` 100 份 `.docx` 简历
- 临时文件（extracted_data.json、write_eval.py）已清理

---

## 第二轮：三项要求修复 + skill 沉淀（zhaopin-resume-saver）

用户指令（原话）："修复刚刚遇到的问题并沉淀入skill ① 修复未满足要求的简历份数就停止 ② 修复获取人员信息时卡死空转情况 ③ 完成任务后，停止浏览器自动下载任务运行"

### 本轮修复的 4 个 bug

**#38 岗位验证误报 mismatch**
- 正则 `'"value":"([^"]*)"'` 贪婪匹配到响应里无关字段 `"no"` → `Active job detected: [no]` → `FATAL: Job mismatch` → exit 1。
- 修复：白名单过滤（排除 no/ok/yes/none/null/true/false/0/1）+ 必须含中文字符 + 长度≥2；探测失败降级为警告继续。

**#39 名字收集返回 0**
- 上一轮 cleanup 关了标签页，重新 navigate 后页面未渲染完就进循环，首轮 evaluate 返回 `ok:false`（"session has no tab"），循环静默空转到 maxScrolls。
- 修复：新增"等待列表容器渲染"预检（20 轮）+ `$evalErrors` 计数 + 连续 3 次失败自动重新 navigate + `Page.bringToFront`。

**#40 ★ `wb-download.json` 文件锁（最关键的基础设施修复）**
- `Send-Web` 固定复用同名临时文件，`curl.exe` 仍持有句柄 → `WriteAllText` 抛 `IOException:"文件正由另一进程使用"` → **首次请求之后所有请求全部失败**。表现极具迷惑性：探测"成功"但点击无响应、No save dialog、卡死。
- 修复：每次请求用 GUID 独立临时文件（`wb-req-{guid}.json`），用毕重试删除 3 次。

**#41 ★★★ 核心根因：智联详情页在「新标签页」打开，session 却一直绑在「列表标签页」**
- 现象：`SaveLocal btn at (1079, 142)`（实为硬编码兜底值）→ `No save dialog after 4 attempts` → 空转；用户描述"一直在翻候选人列表，但没点进去任何一份详情"。
- 定位过程：
  - `list_tabs` 发现**两个标签页**：列表页 `...&tab=recommend#sortType=...` 与详情页 `...&tab=recommend&resumeNumber=<encoded>#sortType=...`
  - 在详情页探测：`.resume-button.position-r` 存在且坐标 **(1079,142)**、`button[保存]` 存在且 **(996,561)** —— 与 config 完全一致（证明 config 坐标没错，是事件打在了错误 tab）
  - 在列表页探测：这两个元素**根本不存在**，且列表持续重渲染，每次探测姓名集合都不同
- 修复：新增 `Switch-ToDetailTab` —— 点击候选人后用 `list_tabs` 枚举，提取 URL 含 `resumeNumber=` 的详情页，用 `navigate`（newTab:false）把 session 的 active tab 切过去，再轮询确认元素就绪。调用点：**3.2.5 步骤**。配套 **3.9 步骤**：每条完成后 navigates 回列表页。
- **核心教训**：当按钮坐标"看起来对但点击无效"时，**优先怀疑事件作用在了错误的标签页**，而不是坐标本身。`list_tabs` 是首选排查工具。

**#42 ★（#41 的续修）详情页取"最新"而非"第一个" + 清理残留标签页**
- 实测发现 `list_tabs` 会返回**多个** `resumeNumber` 标签页（历史遗留旧详情页排在最前）。若按"首个匹配"取，会 navigate 到**上一位候选人**的详情页 → 下错人。
- 另一问题：`navigate`（newTab:false）在 session 绑定失效时会**新建标签页**，导致标签页无限堆积。
- 修复：① `Switch-ToDetailTab` 改为按 **tabId 降序**取最大者（tabId 单调递增 = 最新）；② 新增 `Clear-StaleDetailTabs`，在进入下载阶段前关掉所有残留详情页；③ 3.9 回列表前先 `close_tab` 关掉当前详情页再 navigate，避免堆积。
- 正则验证（对真实 `list_tabs` 输出）：`"tabId"\s*:\s*(\d+)\s*,\s*"url"\s*:\s*"([^"]*resumeNumber=[^"]*)"` → 正确匹配 2 个，降序取到 1861309722（最新）。

### ⚠️ trap 兜底方案已废弃
实测 PowerShell 的 `trap` 会捕获作用域内**任何** terminating error，在长流程脚本中极易被无关的瞬时错误触发，**误杀正常执行中的任务**（下载第 1 条就被中断）。已彻底移除，改用幂等 `Stop-BrowserAutomation` + 多处显式调用。

### 端到端手工验证（成功）
切到详情页后完整跑通：点"存至本地" → 弹窗出现（`bodyCls: km-modal__wrapper--locked`、`saveBtns: 1`）→ 点 "word"（876,384）→ 点"保存"（996,561）→ 产出 `260915_test\何先生_24岁_智联简历_31791.docx`（100694 字节）。

### 本机环境踩坑（重要）
- **本机 PowerShell 工具 stdout 捕获异常**：命令实际执行成功，但工具返回空输出（exit code 0 无内容），或对 Tee-Object 正在写入的文件 `ReadAllBytes` 返回 0 字节。**勿把"无输出"当失败**，改用文件落盘 + Read 复核。
- **Bash 工具缺 coreutils**（`wc`/`tail`/`cat`/`sleep` 均 not found），需用绝对路径调用 PortableGit 自带工具：`/c/Users/34434/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin/{cat,tail,grep,ls}.exe`。
- **PowerShell 输出是 UTF-16LE**，用 iconv 转码才能读：`cat file | iconv -f UTF-16LE -t UTF-8`。
- 禁止 `Start-Process`, 禁止从 Bash 调 `powershell.exe`（安全策略拦截）。
- WebBridge 可用 action 白名单：`navigate, find_tab, find, evaluate, network, snapshot, read_page, click, fill, mouse_click, cdp, key_type, send_keys, screenshot, scroll, save_as_pdf, upload, close_tab, list_tabs, close_session, wait, dialog, select_option, hover, drag`（**没有 `activate_tab`**）。

### 第二轮（续）：真正根因推翻前案 + 最终跑通

> ⚠️ **重要订正**：#41 / #42 的「详情页在新标签页」判断**是误判**。真正根因是 **#44：标签页被遮挡**。

**#44 ★★★ 真正根因：标签页被遮挡 → Chrome 不投递输入事件**

daemon 原始报错（决定性证据）：
```
the click did not reach the page — no pointerdown/mousedown fired.
The tab is likely backgrounded and occluded (another tab in its window is shown,
so its render widget is hidden and real input isn't delivered).
```
- 同窗口多标签页时，非前台标签页的 render widget 被隐藏，**Chrome 不向它投递任何真实输入事件** → 所有 `Input.dispatchMouseEvent` / `mouse_click` **静默失败**（返回 `ok:true` 但页面毫无反应）。
- 修复：新增 `Ensure-TabFocused`（调 CDP `Page.bringToFront`）+ `Invoke-Click`（WebBridge `click` action，DOM 级派发）。**每次交互前都调 `Ensure-TabFocused`**。
- **结论订正**：智联详情面板是**当前标签页内的 `.resume-detail-wrap`**，不需要切标签页。`Switch-ToDetailTab` / `Clear-StaleDetailTabs` / `Test-OnDetailTab` 方案**全部废弃**。

**#45 存至本地按钮延迟挂载 10~15 秒**
- 面板主体 `.resume-detail-wrap`（约 1107×715）先渲染，`.resume-button.position-r` 要等简历正文加载完才挂载。
- 原等待上限 15×500ms=7.5s 远远不够 → 探测失败 → 退回硬编码坐标 → 点了个不存在的按钮 → `No save dialog` → 空转。
- 修复：**两段式等待** —— 先等 `.resume-detail-wrap`（20×500ms），再等按钮可见（**40×750ms ≈ 30 秒**）。

**#46 ★★★ 最终跑通的关键：按钮会"闪烁"，必须"探测到即刻点击"**
- `.resume-button.position-r` 反复挂载/卸载（Vue 渲染过程），探测到"存在"不代表点击那一刻还在。
- 正确序列（4 步，缺一不可）：
  1. CDP `Page.bringToFront`（不置前输入被丢弃）
  2. DOM `click .talent-basic-info__name` 打开面板（面板在当前标签页内渲染）
  3. **探测到按钮可见的那一瞬间立即发 CDP 鼠标序列**（moved → pressed → 80ms → released），**中间不加任何等待**，否则按钮已卸载
  4. 用 `getBoundingClientRect()` 实时坐标，**不要用配置兜底值**；探不到时先判断面板是否还在，收了就重开
- **分工铁律**：**候选人姓名用 DOM click，面板内按钮（存至本地/word/保存）必须用 CDP 真实鼠标事件**，二者不可混用（Vue 组件不响应程序化 `.click()`）。
- 重试次数：3.4 阶段 4 次 → **12 次**。

**第 13 次测试（task `gb6d62`）完整跑通**：`Success: 5 / Failed: 0 / Skipped: 1`、`[DONE] Target reached (5/5)`，产出 5 份 .docx（91568~276810 字节），并正确执行 `[9] Stopping browser automation...`（close_tab + cdp_disable + daemon stop，端口 10086 拒绝连接、`daemon.pid` 已删除）。

### 收尾状态
- `config.json` 已还原正式参数：`DownloadCount=100`、`DownloadDir=...\260914`、`JobName=联想渠道经理-青岛`，**无 BOM**、JSON 有效。
- 测试目录 `260915_test` 已删除。
- `run.ps1` 语法解析通过（`Parser::ParseFile` → `PARSE_OK`，0 errors）。

---

## 评估 skill 模版化改造 + 260914 全量重测（zhaopin-resume-screening）

### 需求
评估结果按 `EvaluationTemplate.xlsx` 模版输出（覆盖内容），用 `output\260914` 的简历测试，结果存回该文件夹。

### 模版结构（实测）
- Sheet1，13 列 A1:M1 表头：简历名称/胜任力评级/简要评价/建议行动/年龄/性别/工作年限/现居住地/学历/求职状态/期望薪资/求职意向/工作经历
- B 列配色（模版实测，与旧版不同）：高=C6EFCE、中=**FFFFB1**、低=**FFD9D9**；斑马纹 F2F7FB；A2 冻结；微软雅黑
- 相比旧 8 列格式新增：D 建议行动、G 工作年限、I 学历、J 求职状态、K 期望薪资；B 改名"胜任力评级"、H 改名"现居住地"

### 代码改动
1. `extract_docx.py / parse_basic_info`：新增解析基本信息行第 3/4/5 段 → `work_years`/`education`/`job_status`（行格式 `男｜38岁(1988年6月)｜11年｜本科｜离职-正在找工作`，按 `[｜|]` 切段）。英文简历自动留空。
2. `fill_excel.py`：新增 `fill_excel_template(json, xlsx)`（加载模版→保留表头→delete_rows 清示例行→写 13 列，样式/斑马纹采样自模版残留行，行高按 L/M 列行数自适应）；新增 `write_evaluation_row(xlsx, row_idx, rating, comment, action)` 按行索引写 B/C/D；新增 `extract_expected_salary()`（从求职意向第一条提取期望薪资）。
3. `SKILL.md`：输出格式章节整章替换为模版版；参数表 `output_xlsx` 默认改模版路径；工作流步骤 3/5、脚本说明、B3 字段清单同步更新。

### 测试结果
- 文件夹实际 **110 份 DOCX**（Glob 默认 limit=100 会截断，勿据此计数！）
- 覆盖填充后逐行写入评估：**高 10 / 中 65 / 低 35**（含樊38/陈28 等重复对，D 列 110 行全填）
- 高匹配唯一 8 人：刘32(IT代理商分销总监)、周29(LED商显)、姜35(美的空调渠道)、张31(家电经销商渠道+UPS)、苏28(医疗器械代理商)、郑28(医疗器械+海尔生物渠道)、陈28(韶音耳机渠道)、隋33(智能硬件渠道体系)
- 重点中档：樊38(联想ThinkPad督导+戴尔，原厂渠道最强，超龄+济南)、穆36(大华/华硕/英特尔，超龄+薪资略超)、薛45(系统集成年3000万，超龄)
- 交付 `260914\联想青岛.xlsx`（模版格式，原模版文件未动）；临时文件已清理

### 新踩坑
- **Glob 默认 limit=100**：>100 个文件的文件夹会静默截断，计数必须用 PowerShell `(gci).Count` 或脚本实测。
- 模版 header fill 是主题色（openpyxl 读出 "Values must be of type str"），不能直接读 RGB 字符串；好在模版填充保留原表头不动，无影响。
- **SKILL.md 已完成正文对齐**：阶段 3 旧写法（`el.click()`、7.5s 等待、单次点击）全部替换为 #44/#46 验证过的流程；阶段 4 的 `trap` 兜底改为"不要用 trap + 幂等函数多处显式调用"；问题 #41/#42 标注为**[已被 #44 推翻 — 误判]**并保留方法论参考价值。


## 模版化输出格式微调（第二轮，用户反馈）

1. **全表垂直居中**：用户要求所有单元格垂直居中。检查发现模版本身 A/C/E/G/L/M 列为 vertical=top，已对 `260914\联想青岛.xlsx` 全部 111 行 × 13 列（含表头）改为 vertical=center（保留自动换行与水平对齐），读回校验 0 个非居中单元格。
2. **D 列建议行动去标签化**：废弃"优先约面/可备选/暂不推进"三固定标签，改为按岗位要求与候选人具体情况自由撰写的结论性短句（如"渠道销售经验直接对口""经验年限尚浅，可面谈培养空间""省外+薪资严重不符"），110 行全部重写。
3. **skill 同步**：`fill_excel.py` 的 fill_excel_template/write_evaluation_row 对齐改为默认垂直居中；`SKILL.md` D 列章节改为「建议行动写法」（含 9 条风格示例 + 写法要求：一句话 10~30 字、先核心判断后顾虑/亮点、必须落到具体差异点）。
- 评级分布不变：高 10 / 中 65 / 低 35。临时文件已清理。

## 联想渠道经理-青岛 全链路完成（26091516，#56 新架构首次完整实战）

- 16:16 用户下达任务：重新下载前 100 份至 `output/26091516` 并评估生成联想青岛.xlsx。
- **"文件夹消失"之谜解开**：260914 / 26091512(87份) / 26091515(22份) 均被移入 `output/` 目录下，数据未丢失（此前回收站无痕迹是因为是移动而非删除）。
- 下载：config.json 改 100 份 + DownloadDir=output\26091516（Edit 后 Read 复核落盘）；复用计划任务 WbResumeDL（wrapper 读 %TEMP%\wb_wrapper_paths.txt），16:19 启动 → 16:27 已 9 份 → 16:59:34 **DONE：ok=100 / fail=0 / skip=0**，全程约 33 分钟（~20s/份），#56 新架构（卡片登记+顺序推进+单轮异常保护+list_tabs 自检）全程零干预。
- 评估：`pipeline.py prepare`（100 提取 → 姓名+年龄去重跳 8 份 → 92 行）→ AI 逐份评估（**高 2 / 中 71 / 低 19**；超龄>35 封顶"中"规则约束了大量快消/地产渠道候选人；薪资 6-10k 对比期望）→ `evaluate` 写入 B/C+色阶+verify 通过（缺评 0）。
- 摘要方法：build_digest.py 生成 digest.jsonl（92 行、单行 <1900 字符防 Read 截断、desc 截断兜底），分 3 次 Read 完成全量阅卷；workdir=%TEMP%\wb_screen_26091516（extracted_data.json/rows.json/evaluations.json）。
- 高匹配 2 人：**张女士30**（鼎信通讯消防，20+经销商签约-回款全周期管理，青岛，8-10k）、**杨先生24**（云管家数据，云服务器/AI 工具企业客户开发，青岛，8-16k）。最对口但受限者：刘27 海信家电渠道（期望 1.3 万起）、林45 电子元器件 20 年（超龄）、梁32 信创集成商渠道（现居威海）。
- 交付：`output/26091516/联想青岛.xlsx`（30,605 字节，92 数据行 + 100 份 docx + _summary.json + _run_log.txt）。

## skill 时序优化 #57：保存后关面板 3200ms + 关闭验证重试（用户指令）

- 需求：点击保存按钮后关闭模态弹窗等待 1000ms→3200ms；已关闭→继续下一位候选人；未关闭→每 1000ms 重试。
- 改动：zhaopin-page.ps1 新增 `Close-ModalVerified`（关闭一轮→探测 `.km-modal--open`→未关每 1000ms 重试，上限 10 次超限 WARN 不阻断）+ 作用域契约注释补 2 参数；run.ps1 `$Config` 增 ModalRetryWaitMs=1000/ModalCloseRetryMax=10、SaveCloseWaitMs=3200，3.6 与 3.8/3.9 双保险段改用验证函数；SKILL.md 时序参数表 3 行 + 架构表 + #57 记录 + 修正问题记录区重复编号 52→52b + 可移植性章节陈旧描述纠正（"函数内建于 run.ps1"→dot-source lib，#56 遗留）。
- **重要排障新知：同一消息内对同一文件的多个 Edit 只有最后一个生效**（批量 run.ps1 3 处丢 2 处、SKILL.md 5 处丢 4 处——均报成功但被最后写入覆盖）；已强化 MEMORY.md 铁律：同文件多处修改必须逐消息拆分+逐一复核。
- 终验：run.ps1 / zhaopin-page.ps1 Parser 语法 0 错误、UTF-8 BOM 完好；无运行中下载任务（26091516 已 DONE），改动对下次运行生效。

## 26091517 ai产品经理 批次（18:00 起）

- 用户指令：从 rd6.zhaopin.com 下载 ai产品经理 岗位前 200 份简历并评估，输出 output/26091517 ai产品经理/ai产品经理.xlsx（薪资 12k-20k）。
- **岗位定位**：ai产品经理 jobNumber=CC136786060J41031060702（职位管理页 job-pane 点击切换后从 URL 读取）。联想渠道经理-青岛=CC136786060J41007428302——首跑曾沿用旧 URL 下载错岗位 6+ 份（已删除），用户明确两个问题要求修复沉淀。
- **#58 问题1 修复**：run.ps1 岗位探测失败从 WARN 放行改为 FATAL exit 1（config AllowUnverifiedJob=true 可豁免）；SKILL.md 新增"岗位确认铁律"。
- **#59 问题2 修复**：实测 scrollTop 赋值不触发推荐列表懒加载（scrollHeight 恒 5350 → 21 份误判耗尽）；CDP mouseWheel 触发加载（scrollHeight 涨至 66714+）。[D] 分支改 CDP 滚轮 + \ 增长判据（增长>200px 不算空轮）。#33 的"scrollTop 可靠"结论已过时。
- **#60**：Send-Web curl 加 -m 20 超时（此前 daemon stop 挂起导致清理环节永久卡死、_summary.json 不落盘）。
- 断点续传修正：目录已有 26 份时 DownloadCount 改 174（200-26），防超量。